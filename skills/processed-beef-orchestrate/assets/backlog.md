# Backlog

One line per pending change; each line links to its change directory under
`docs/changes/`. Created only when more than one change is pending or active.
Items never duplicate plan tasks.

- P1 | active | <desired outcome> | [change](changes/<change-id>/)
