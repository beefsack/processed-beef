# Backlog

Template for `docs/backlog.md`. One line per pending change; each line links to
its change directory under `docs/changes/` and identifies cross-change
dependencies. Created only when more than one change is pending or active.
Items never duplicate plan tasks.

- P1 | active | dependencies: <none or change IDs> | <desired outcome> | [change](changes/<change-id>/)
